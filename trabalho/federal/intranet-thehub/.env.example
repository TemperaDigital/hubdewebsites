# Copie para .env e preencha. NUNCA commitar o .env (já está no .gitignore).
# Apenas a ANON KEY (pública). A service_role JAMAIS vai para o frontend.
VITE_SUPABASE_URL=https://spcdatabase.fguerra.ia.br
VITE_SUPABASE_ANON_KEY=
